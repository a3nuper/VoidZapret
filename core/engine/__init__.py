"""VoidEngine — собственный движок обхода DPI на WinDivert (без winws.exe).

Перехватывает исходящие TLS ClientHello и применяет десинхронизацию (пока —
split внутри SNI), ломая SNI-сигнатуру DPI. Это фундамент независимого от
Flowseal метода; будет развиваться (fake-пакеты, seqovl, авто-калибровка).
"""
